# OpenGameMate v0.1.0 用户 RC 验收清单

**当前状态：** `Ready for Public Testing`

用户已完成真实游戏环境 RC 验收，并确认登录、Voice、角色初始化、截图提交、Voice 连续性、画面回应、截图延迟机制和长时间实际使用均通过。以下记录只写入用户明确确认的事实；未单独提供结果的异常、安装包和远程规则项目不会被虚构为已验证。

本清单由用户在真实游戏环境中执行。测试画面应避免隐私内容；OpenGameMate 不应记录聊天内容、回复、账号、Cookie、Token、音频或截图内容。

## 验收信息

| 项目 | 填写内容 |
| --- | --- |
| 测试人 | 用户真实环境验收 |
| 测试日期 | 2026-07-14 |
| Git/构建版本 | `4c671bf` |
| Windows 版本 |  |
| .NET Desktop Runtime |  |
| WebView2 Runtime |  |
| 游戏及显示模式 |  |
| 主显示器分辨率 |  |
| ChatGPT 账号能力/套餐（可选） |  |

结果填写规则：在“结果”中填写 `PASS`、`FAIL` 或 `BLOCKED`；实际结果不得粘贴聊天正文、账号信息、Token、音频或截图内容。失败时只记录时间、程序状态、稳定错误码和不含隐私的现象。

## A. 基础启动

| ID | 测试项 | 测试步骤 | 预期结果 | 实际结果 | 结果 |
| --- | --- | --- | --- | --- | --- |
| A1 | 程序启动 | 启动 `OpenGameMate.App.exe`，不点击其他功能。 | 主窗口正常出现；状态可读；启动时不创建截图，不自动恢复上次运行状态。 | 用户完成真实 RC 启动流程。 | PASS |
| A2 | WebView2 打开 | 点击“打开 ChatGPT”。 | 官方 ChatGPT 页面直接嵌入主窗口右侧，不产生第二个顶层窗口，并使用 OpenGameMate 独立用户数据目录。 | 内嵌 ChatGPT 页面正常打开并用于后续登录。 | PASS |
| A3 | ChatGPT 登录 | 由用户手动完成登录；不要向 OpenGameMate 输入凭据。 | 登录流程可完成；主程序不显示或记录账号、Cookie、Token。 | 用户确认 ChatGPT 登录正常。 | PASS |
| A4 | Voice 开启 | 在 ChatGPT 页面由用户开启 Voice，再回到主窗口确认。 | 麦克风权限由用户控制；Voice 可正常交谈；OpenGameMate 不录音、不保存或识别音频内容。 | 用户确认 Voice 正常运行。 | PASS |
| A5 | 角色初始化 | 选择发送完整角色设定。 | 设定仅提交一次；ChatGPT 接收设定；OpenGameMate 不读取回复。 | 用户确认角色初始化正常。 | PASS |

## B. 核心体验

| ID | 测试项 | 测试步骤 | 预期结果 | 实际结果 | 结果 |
| --- | --- | --- | --- | --- | --- |
| B1 | 手动截图 | 保持游戏在前台，点击“立即发送”。 | 捕获主显示器并压缩到不超过 1920×1080；图片和固定提示进入目标 composer 并成功提交；临时文件随后清理。 | 用户确认截图发送正常。 | PASS |
| B1a | 全局主动截图快捷键 | 在陪玩控制区录入新的修饰键组合，重启后让游戏保持前台并按一次。再尝试一个已占用组合。 | 新组合持久化且只触发一次手动截图；不抢焦点、不移动鼠标；冲突时保留旧组合。 |  | PASS / FAIL |
| B1b | 空闲等待档位 | 依次选择 10、15、30、60 秒，重启确认最后选择持久化；在一个等待中的轮次改档。 | 只接受四个档位；默认 10 秒；重启保留选择；等待中的轮次保持创建时档位，下一轮使用新档位。 | 用户确认截图延迟选择与生效机制正常。 | PASS |
| B2 | 首次自动截图 | 选择一个空闲等待档位，开始陪玩，停止说话并等待 ChatGPT 网页音频结束。 | 完整所选时长内不截图、不挂附件、不写文字；页面和网页音频连续稳定达到所选时长后捕获最新画面并发送一次。 | 用户确认空闲后自动截图与发送正常。 | PASS |
| B3 | 后续自动截图 | 首次发送后保持连续空闲，再恢复一次真实交谈并再次停止。 | 同一连续空闲段不重复发送；恢复活动后重新计时，新空闲段满所选时长时最多触发一次；容量始终为 1。 | 用户确认实际使用中的自动截图连续运行正常。 | PASS |
| B4 | Voice 不中断 | 让 Voice 持续发言，然后观察发言结束后的自动行为。 | 发言期间不操作 composer；网页音频停止且页面状态连续稳定达到所选时长后才截图和准备发送；不抢占播放尾音。 | 用户确认自动截图不会打断 Voice。 | PASS |
| B5 | AI 根据画面回应 | 在安全空闲时完成一次截图发送，继续保持 Voice。 | ChatGPT 能看到最新图片并结合画面继续 Voice 回应；OpenGameMate 不读取回应。 | 用户确认 AI 能够根据游戏画面回应。 | PASS |
| B6 | 主动话题能力 | 保持用户暂时不说话，让安全空闲时的自动截图完成发送。 | 随图提示要求 ChatGPT 根据最新画面自然发起一个简短话题，Voice 能正常回应。 |  | PASS / FAIL |
| B7 | 游戏前台与鼠标 | 记录发送前的前台窗口和鼠标位置，完成手动及自动发送。 | 截图、附件、文字和提交全过程不移动鼠标、不抢占游戏前台焦点。 |  | PASS / FAIL |

## C. 稳定性

| ID | 测试项 | 测试步骤 | 预期结果 | 实际结果 | 结果 |
| --- | --- | --- | --- | --- | --- |
| C1 | 30 分钟运行 | 在真实游戏中连续运行 30 分钟，保持正常 Voice 对话并观察自动发送。 | 程序与 WebView2 保持可用；无重复任务、截图堆积、过时补发或连续上传失效。 | 用户确认长时间实际使用通过。 | PASS |
| C2 | 2 小时运行 | 开始一次新的两小时连续运行，每 30 分钟记录资源和累计事件。 | 两小时内程序、WebView2 和调度保持可用，Voice 与图片闭环可继续工作。 | 用户确认长时间实际使用通过。 | PASS |
| C3 | 内存变化 | 在长稳测试开始及各记录点查看 OpenGameMate 和 WebView2 相关内存。 | 数值已完整记录；未出现持续失控增长、内存耗尽或因此导致的不可用。 |  | PASS / FAIL |
| C4 | CPU 变化 | 在长稳测试开始及各记录点查看 OpenGameMate CPU，并记录异常持续占用。 | 数值已完整记录；空闲和正常运行时没有导致游戏或 Voice 不可用的持续异常占用。 |  | PASS / FAIL |
| C5 | WebView2 稳定性 | 观察页面响应、进程失败、窗口恢复、图片上传和 Voice 状态。 | WebView2 保持可用；故障有界且可诊断；没有恢复循环、随机点击或重复上传。 |  | PASS / FAIL |

### C1 记录表：30 分钟运行

1. 在真实游戏中开始陪玩并连续运行 30 分钟。
2. 保持正常 Voice 对话，记录自动发送次数、上传失败、Voice 中断、页面异常及前台变化。
3. 在开始、15 分钟和结束时记录资源观察值。

| 观察项 | 开始 | 15 分钟 | 结束/累计 |
| --- | --- | --- | --- |
| OpenGameMate 内存 |  |  |  |
| OpenGameMate CPU |  |  |  |
| WebView2 相关内存（观察值） |  |  |  |
| 自动发送成功次数 | — | — |  |
| 图片上传失败次数 | — | — |  |
| Voice 非预期中断次数 | — | — |  |
| 重复任务/截图堆积 | — | — |  |

- 预期结果：程序与 WebView2 保持可用；无持续失控的资源增长；无重复定时任务、截图堆积或过时补发；普通失败不立即重试。
- 实际结果：用户确认 30 分钟真实使用通过；未提供资源数值明细。
- 结果：`PASS`

### C2 记录表：2 小时运行

1. 在真实游戏中开始新的两小时连续运行。
2. 在开始、30、60、90 和 120 分钟记录资源观察值和累计事件。
3. 结束后检查 WebView2、自动调度、临时截图清理、Voice 连续性和诊断日志。

| 观察项 | 开始 | 30 分钟 | 60 分钟 | 90 分钟 | 120 分钟/累计 |
| --- | --- | --- | --- | --- | --- |
| OpenGameMate 内存 |  |  |  |  |  |
| OpenGameMate CPU |  |  |  |  |  |
| WebView2 相关内存（观察值） |  |  |  |  |  |
| 自动发送成功次数 | — | — | — | — |  |
| 图片上传失败次数 | — | — | — | — |  |
| Voice 非预期中断次数 | — | — | — | — |  |
| WebView2 异常/恢复次数 | — | — | — | — |  |
| 重复任务/截图堆积 | — | — | — | — |  |

- 预期结果：两小时内程序、WebView2 和调度保持可用；无持续失控的资源增长；无重复任务、截图堆积、过时补发或连续上传失效。
- 实际结果：用户确认长时间真实使用通过；未提供资源数值明细。
- 结果：`PASS`

## D. 异常与恢复

| ID | 测试项 | 测试步骤 | 预期结果 | 实际结果 | 结果 |
| --- | --- | --- | --- | --- | --- |
| D1 | 网络断开 | 陪玩运行时断开网络，等待一次空闲触发机会，再恢复网络。 | 失败有界且可诊断；不随机点击、不快速重试、不发送陈旧截图；网络恢复后仅在新的安全空闲窗口继续。 |  | PASS / FAIL |
| D2 | ChatGPT 刷新 | 刷新 ChatGPT 页面，观察适配与 Voice 状态，再按界面要求重新确认。 | 页面未恢复到精确安全状态前失败关闭；不误触控件；需要时要求用户重新确认 Voice。 |  | PASS / FAIL |
| D3 | 浏览器关闭 | 陪玩运行时点击“关闭网页并结束 Voice”。 | 陪玩安全停止并恢复内嵌占位页；不会自动发送；用户重新打开页面后需要明确确认 Voice。 |  | PASS / FAIL |
| D4 | 程序重启 | 正常退出并重新启动；另记录一次异常退出后的启动表现。 | 设置可恢复；运行状态和待发送任务不自动恢复；临时截图不残留；不会静默发送。 |  | PASS / FAIL |
| D5 | 图片额度不足 | 仅在账号真实出现图片额度限制时观察；不得绕过或人为攻击限制。 | 进入 `VoiceOnly`；不立即重试、不新建对话绕过、不随机尝试；若无法触发则记录 `BLOCKED/NOT OBSERVED`。 |  | PASS / FAIL / BLOCKED |

## E. 验收结论

| 项目 | 结论 |
| --- | --- |
| 基础启动 | PASS |
| 核心体验 | PASS |
| 30 分钟稳定性 | PASS（用户确认） |
| 2 小时稳定性 | PASS（用户确认） |
| 异常与恢复 | 未包含在本次用户提供的验收事实中 |
| 安装包测试 | NOT AVAILABLE |
| 远程适配规则发布 | NOT PUBLISHED |
| 是否建议公开测试 | YES |

最终备注：用户已完成真实 RC 验收；基于明确确认的登录、Voice、角色初始化、截图、延迟、画面回应和长时间使用结果，当前版本状态更新为 `Ready for Public Testing`。异常矩阵、安装包和远程规则保持公开测试阶段的已知跟进项，不伪造通过记录。

测试人签字/确认：用户已在对话中明确确认　日期：2026-07-14
